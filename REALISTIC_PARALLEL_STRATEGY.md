# 有限并行策略：务实方案

## 核心原则：并行任务必须满足"独立性三要素"

### 1. 文件级独立性
- ✅ **允许**: Agent A修改`internal/lsp/`, Agent B修改`internal/mcp/`
- ❌ **禁止**: 两个Agent修改同一目录下的文件

### 2. 接口级独立性
- ✅ **允许**: Agent A实现新接口, Agent B使用已有接口
- ❌ **禁止**: 两个Agent修改同一接口定义

### 3. 数据级独立性
- ✅ **允许**: Agent A操作session表, Agent B操作lsp_diagnostics表
- ❌ **禁止**: 两个Agent同时修改数据库schema

## 推荐实施方案

### 方案一：预定义并行模式（保守，推荐）

**仅支持以下安全并行场景**：

1. **跨子系统任务**
   ```
   Task A: LSP性能优化
   Task B: 新增MCP服务器支持
   Task C: TUI主题系统重构
   冲突概率: <5%
   ```

2. **独立工具开发**
   ```
   Task A: 实现代码格式化工具
   Task B: 实现代码搜索工具
   Task C: 实现依赖分析工具
   冲突概率: <10%
   ```

3. **Bug修复 + 新功能**
   ```
   Task A: 修复数据库连接泄漏
   Task B: 实现新的图片压缩功能
   冲突概率: <3%
   ```

**不支持的高风险场景**：
- 同一模块的重构和功能添加
- 数据库schema并行修改
- 核心类型定义并行修改

### 方案二：渐进式并行（激进，风险高）

**第一阶段（0-3个月）**: 完全禁用并行
- 专注解决安全漏洞和MIT重新实现
- 积累单任务执行数据

**第二阶段（3-6个月）**: 启用跨子系统并行
- 仅允许LSP/MCP/TUI/DB四个子系统并行
- 自动冲突检测：如果git worktree合并失败，立即回滚

**第三阶段（6-12个月）**: AI辅助依赖分析
- 使用LSP分析函数调用图
- 构建模块依赖DAG（有向无环图）
- 仅并行独立子图中的任务

## 现实约束

### 技术约束
- Git worktree合并冲突率: 预计15-30%（即使是"独立"任务）
- 人工解决冲突时间: 平均15-45分钟/次
- **ROI计算**: 如果冲突率>20%，并行反而降低效率

### 用户体验约束
- 用户需要理解哪些任务可以并行（认知负担）
- 冲突时需要人工介入（打断工作流）
- 调试变复杂：不知道是哪个Agent引入的Bug

## 最终建议

**短期（0-6个月）**:
- ❌ 不实施并行工作流
- ✅ 优化单任务执行速度（通过缓存、并行工具调用）
- ✅ 改进会话切换UX（快捷键、会话树）

**中期（6-12个月）**:
- ✅ 实施跨子系统并行（LSP/MCP/TUI/DB）
- ✅ 提供"并行适配性检查"命令
- ✅ 自动冲突检测和回滚

**长期（12-24个月）**:
- 🤔 评估AI辅助依赖分析的可行性
- 🤔 考虑基于任务历史的冲突预测

## 替代方案：优化单任务工作流

**投入更少，收益更确定的方向**：

1. **会话快速切换**
   - 快捷键在会话间跳转（<100ms延迟）
   - 会话标签/分组/搜索
   - 预加载相邻会话

2. **任务分解建议**
   - AI分析复杂任务，推荐拆分为3-5个子任务
   - 每个子任务独立会话执行
   - 用户手动控制执行顺序

3. **后台任务队列**
   - 添加任务到队列，顺序执行
   - 长任务（>5分钟）后台运行
   - 完成时通知用户

**结论**: 与其冒高风险实施通用并行，不如优化单任务体验和手动并行控制
